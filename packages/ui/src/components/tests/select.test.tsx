import { describe, it } from "vitest";

// TODO: adapt once Select API/DOM is finalized.
// Current DOM shows a visually-hidden native <select> without a visible trigger.
describe.skip("Select", () => {
  it("opens and selects an option", () => {});
});